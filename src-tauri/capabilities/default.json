{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions required by the bundled local bootstrap window only.",
  "windows": ["main"],
  "permissions": ["core:event:default"]
}
