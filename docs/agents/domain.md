# Domain docs

This repository uses a single-context domain documentation layout.

- `CONTEXT.md` is the shared domain glossary and current architectural context.
- `docs/adr/` stores durable architectural decisions.
- Read both before changes that alter domain terms or long-lived architecture.
