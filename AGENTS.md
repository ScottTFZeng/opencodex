## Agent skills

### Issue tracker

Issues live locally under `.scratch/<feature>/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the standard five-label vocabulary. See `docs/agents/triage-labels.md`.

### Domain docs

Use root `CONTEXT.md` and `docs/adr/` as a single-context layout. See `docs/agents/domain.md`.
